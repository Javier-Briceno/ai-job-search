{
  "name": "absolventa-cli",
  "version": "1.0.0",
  "description": "CLI for searching jobs on Absolventa, the German student/graduate job board - zero runtime dependencies. No ToS restriction (robots.txt only blocks one unrelated path).",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "absolventa-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 30000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "1.3.14"
  }
}
